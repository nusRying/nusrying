name: Update Space Shooter Game

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-game:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout profile repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Generate contribution space shooter
        uses: czl9707/gh-space-shooter@eea3dd4d83e464ec7c4153e09e303952b924f5c5
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          username: nusRying
          output-path: 'game.gif'
          strategy: 'random'
